import * as React from "react";
import { I18NValue } from "@webiny/app-i18n/components";
import { Input as UiInput } from "@webiny/ui/Input";

var Input = function Input(props) {
  return /*#__PURE__*/React.createElement(UiInput, Object.assign({}, props, props.bind, {
    onChange: function onChange(value) {
      if (props.type === "number") {
        value = parseFloat(value);
      }

      return props.bind.onChange(value);
    },
    label: I18NValue({
      value: props.field.label
    }),
    placeholder: I18NValue({
      value: props.field.placeholderText
    }),
    description: I18NValue({
      value: props.field.helpText
    }),
    type: props.type,
    trailingIcon: props.trailingIcon
  }));
};

export default Input;
//# sourceMappingURL=Input.js.map