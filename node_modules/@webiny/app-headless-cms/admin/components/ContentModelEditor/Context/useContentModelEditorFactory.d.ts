import { CmsEditorFieldsLayout, CmsEditorField, FieldLayoutPositionType, CmsEditorFieldTypePlugin } from "@webiny/app-headless-cms/types";
declare const _default: (ContentModelEditorContext: any) => () => {
    apollo: any;
    data: any;
    state: any;
    getContentModel(id: string): Promise<any>;
    saveContentModel: (rawData?: any) => Promise<any>;
    /**
     * Set form data by providing a callback, which receives a fresh copy of data on which you can work on.
     * Return new data once finished.
     * @param setter
     * @param saveContentModel
     */
    setData(setter: Function, saveContentModel?: boolean): Promise<any>;
    /**
     * Returns fields list or complete layout with fields data in it (not just field IDs).
     * @param layout
     * @returns {*}
     */
    getFields(layout?: boolean): CmsEditorFieldsLayout | CmsEditorField[];
    /**
     * Return field plugin.
     * @param query
     * @returns {void|?CmsEditorField}
     */
    getFieldPlugin(query: object): CmsEditorFieldTypePlugin;
    /**
     * Checks if field of given type already exists in the list of fields.
     * @param query
     * @returns {boolean}
     */
    getField(query: object): CmsEditorField;
    /**
     * Inserts a new field into the target position.
     * @param data
     * @param position
     */
    insertField(data: CmsEditorField, position: FieldLayoutPositionType): void;
    /**
     * Moves field to the given target position.
     * @param field
     * @param position
     * @param data
     */
    moveField({ field, position }: {
        field: string | CmsEditorField;
        position: FieldLayoutPositionType;
    }): void;
    /**
     * Moves row to a destination row.
     * @param source
     * @param destination
     */
    moveRow(source: number, destination: number): void;
    /**
     * Updates field.
     * @param fieldData
     */
    updateField(fieldData: any): void;
    /**
     * Deletes a field (both from the list of field and the layout).
     * @param field
     */
    deleteField(field: CmsEditorField): void;
    /**
     * Returns row / index position for given field.
     * @param field
     * @returns {{index: number, row: number}|{index: null, row: null}}
     */
    getFieldPosition(field: string | CmsEditorField): FieldLayoutPositionType;
};
export default _default;
