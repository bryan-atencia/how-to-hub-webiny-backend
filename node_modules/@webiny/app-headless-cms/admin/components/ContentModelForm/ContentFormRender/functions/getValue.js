import get from "lodash/get";

var getValue = function getValue(_ref) {
  var bind = _ref.bind,
      locale = _ref.locale,
      field = _ref.field,
      index = _ref.index;
  var values = get(bind, "value.values");

  if (!Array.isArray(values)) {
    values = [];
  }

  var valueForLocale = values.find(function (item) {
    return item.locale === locale;
  });

  if (valueForLocale) {
    valueForLocale = valueForLocale.value;
  }

  if (field.multipleValues) {
    if (!Array.isArray(valueForLocale)) {
      valueForLocale = [];
    }

    if (index >= 0) {
      return valueForLocale[index] || null;
    }

    return valueForLocale;
  }

  return valueForLocale || null;
};

export default getValue;
//# sourceMappingURL=getValue.js.map