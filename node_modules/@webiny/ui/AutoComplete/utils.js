export var getOptionValue = function getOptionValue(option, props) {
  if (option) {
    return props.useSimpleValues ? option : option[props.valueProp];
  }

  return option;
};
export var getOptionText = function getOptionText(option, props) {
  if (option) {
    return props.useSimpleValues ? option : option[props.textProp];
  }

  return option;
};
//# sourceMappingURL=utils.js.map