import * as React from "react";
import { Plugin } from "@webiny/plugins/types";
export declare type AppTemplateRenderer = (children: React.ReactElement) => React.ReactElement;
export declare type AppTemplateRendererPlugin = Plugin & {
    type: "app-template-renderer";
    render: AppTemplateRenderer;
};
