import * as React from "react";
import warning from "warning";
import { getPlugins } from "@webiny/plugins";

var ImagesList = function ImagesList(props) {
  var _props$data = props.data,
      data = _props$data === void 0 ? {} : _props$data;
  var component = data.component;
  var plugins = getPlugins("pb-page-element-images-list-component");
  var pageList = plugins.find(function (cmp) {
    return cmp.componentName === component;
  });

  if (!pageList) {
    warning(false, "Pages list component \"".concat(component, "\" is missing!"));
    return null;
  }

  var ListComponent = pageList.component;

  if (!ListComponent) {
    warning(false, "React component is not defined for \"".concat(component, "\"!"));
    return null;
  }

  return null;
};

export default /*#__PURE__*/React.memo(ImagesList);
//# sourceMappingURL=ImagesList.js.map