/// <reference types="react" />
import { DialogOnClose } from "@webiny/ui/Dialog";
declare type Props = {
    open: boolean;
    onClose: DialogOnClose;
    onSubmit: Function;
    revisions: Array<{
        id: string;
        title: string;
        version: number;
        published: boolean;
    }>;
    selected: string;
};
declare const PublishRevisionDialog: ({ open, onClose, onSubmit, revisions, selected }: Props) => JSX.Element;
export default PublishRevisionDialog;
