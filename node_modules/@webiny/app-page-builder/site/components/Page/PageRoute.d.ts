/// <reference types="react" />
import { Location } from "history";
declare type PageRouteProps = {
    location: Location;
};
declare function PageRoute({ location }: PageRouteProps): JSX.Element;
export default PageRoute;
