/// <reference types="react" />
import { PbPageData } from "@webiny/app-page-builder/types";
declare type PageRenderProps = {
    error?: any;
    loading?: boolean;
    data?: PbPageData;
};
declare function PageRender({ loading, data, error }: PageRenderProps): JSX.Element;
export default PageRender;
