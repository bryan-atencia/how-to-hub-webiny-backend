import Slate from "./Slate";
/**
 * Create Slate editor value object using given text and typography.
 *
 * @param text
 * @param typography
 * @returns {{object, document}}
 */
export declare const createValue: (text: any, typography?: string) => {
    object: string;
    document: {
        object: string;
        nodes: {
            object: string;
            type: string;
            nodes: {
                object: string;
                leaves: any[];
            }[];
        }[];
    };
};
export default Slate;
