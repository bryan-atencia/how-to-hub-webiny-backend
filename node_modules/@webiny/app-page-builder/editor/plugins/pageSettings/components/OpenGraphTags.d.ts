/// <reference types="react" />
import { BindComponent } from "@webiny/form/Bind";
declare type Props = {
    prefix: string;
    value: Array<{
        [key: string]: string;
    }>;
    onChange: Function;
    Bind: BindComponent;
};
declare const MetaTags: ({ prefix, value, onChange, Bind, ...other }: Props) => JSX.Element;
export default MetaTags;
