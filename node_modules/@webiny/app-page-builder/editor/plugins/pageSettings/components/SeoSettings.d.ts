/// <reference types="react" />
import { BindComponent } from "@webiny/form/Bind";
declare type Props = {
    Bind: BindComponent;
};
declare const SocialSettings: ({ Bind }: Props) => JSX.Element;
export default SocialSettings;
