/// <reference types="react" />
/**
 * TODO: this entire component can be further optimized (see the Chrome Profiler) to avoid unnecessary elements re-renders.
 */
declare const Row: ({ element }: {
    element: any;
}) => JSX.Element;
export default Row;
