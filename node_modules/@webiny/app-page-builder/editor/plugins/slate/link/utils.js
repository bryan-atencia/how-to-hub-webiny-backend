export var TYPE = "link";
export var isLink = function isLink(i) {
  return i.type === TYPE;
};
export var getLinkRange = function getLinkRange(change, selection) {
  change.select(selection);
  var link = change.value.inlines.find(isLink);

  if (!link) {
    return selection;
  } // Create full link range


  var firstText = link.getFirstText();
  var lastText = link.getLastText();
  return {
    anchor: {
      path: change.value.document.getPath(firstText.key),
      offset: 0
    },
    focus: {
      path: change.value.document.getPath(lastText.key),
      offset: lastText.text.length
    }
  };
};
//# sourceMappingURL=utils.js.map