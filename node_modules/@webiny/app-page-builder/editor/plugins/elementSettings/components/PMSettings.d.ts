/// <reference types="react" />
/**
 * PMSettings (Padding/Margin settings).
 * This component is reused in Padding and Margin plugins since the behavior of both CSS attributes is the same.
 */
declare type PMSettingsProps = {
    value: any;
    valueKey: string;
    advanced: boolean;
};
declare const _default: import("react-redux").ConnectedComponent<(props: PMSettingsProps) => JSX.Element, any>;
export default _default;
