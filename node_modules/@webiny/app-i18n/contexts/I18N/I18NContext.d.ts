import React from "react";
import { QueryResult } from "react-apollo";
export declare const getI18NInformation: import("graphql").DocumentNode;
export declare const I18NContext: React.Context<any>;
export declare type I18NContextValue = {
    refetchLocales(variables?: {
        [key: string]: any;
    }): Promise<QueryResult>;
    state: {
        initializing: boolean;
        currentLocale?: {
            id: string;
            code: string;
        };
        locales: Array<{
            id: string;
            code: string;
            default: boolean;
        }>;
    };
};
export declare type I18NProviderProps = {
    children?: React.ReactNode;
    loader?: React.ReactElement;
};
export declare const I18NProvider: (props: I18NProviderProps) => JSX.Element;
