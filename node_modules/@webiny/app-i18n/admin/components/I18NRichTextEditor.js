import _toConsumableArray from "@babel/runtime/helpers/toConsumableArray";
import React from "react";
import { RichTextEditor } from "./RichTextEditor";
import { getPlugins } from "@webiny/plugins";

var I18NRichTextEditor = function I18NRichTextEditor(props) {
  var plugins = React.useMemo(function () {
    return getPlugins("i18n-input-rich-text-editor").map(function (plugin) {
      return plugin.plugin;
    });
  }, []);

  if (Array.isArray(props.plugins)) {
    plugins = [].concat(_toConsumableArray(plugins), _toConsumableArray(props.plugins));
  }

  return /*#__PURE__*/React.createElement(RichTextEditor, Object.assign({}, props, {
    plugins: plugins
  }));
};

export default I18NRichTextEditor;
//# sourceMappingURL=I18NRichTextEditor.js.map