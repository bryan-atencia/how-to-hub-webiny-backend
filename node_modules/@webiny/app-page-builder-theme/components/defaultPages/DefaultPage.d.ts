import { ReactNode } from "react";
export declare type DefaultPageProps = {
    title?: ReactNode;
    message?: ReactNode;
};
export default function DefaultPage({ title, message }: DefaultPageProps): JSX.Element;
