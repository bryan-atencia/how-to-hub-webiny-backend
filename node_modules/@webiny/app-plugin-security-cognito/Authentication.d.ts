import React from "react";
export declare type AuthenticationProps = {
    onIdToken(idToken: string): void;
    [key: string]: any;
};
declare const Authentication: React.FC<AuthenticationProps>;
export default Authentication;
