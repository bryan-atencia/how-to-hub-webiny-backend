import React from "react";
import { connect } from "@webiny/app-page-builder/editor/redux";
import { getElement } from "@webiny/app-page-builder/editor/selectors";

var TypeFormEmbed = function TypeFormEmbed(props) {
  var source = props.element.data.source;

  if (!source || !source.url) {
    return /*#__PURE__*/React.createElement("span", null, "You must configure your embed in the settings!");
  }

  return /*#__PURE__*/React.createElement("iframe", {
    frameBorder: "0",
    src: source.url,
    style: {
      height: "100%",
      width: "100%"
    }
  });
};

export default connect(function (state, props) {
  return {
    element: getElement(state, props.elementId)
  };
})(TypeFormEmbed);
//# sourceMappingURL=TypeFormEmbed.js.map