/// <reference types="react" />
declare const Button: ({ children, onClick, disabled, loading }: {
    children: any;
    onClick: any;
    disabled?: boolean;
    loading?: boolean;
}) => JSX.Element;
export default Button;
